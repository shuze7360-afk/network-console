# 更新说明

## 2.0.0（2026-09-19）

**不兼容变化：MCP `net_fix` 不再代为执行修复。**

- 后台与 AI 会话只诊断、提醒与解释；网络修改只能由用户在控制台界面点击触发。
  所有修改入口默认拒绝后台来源（`manual-required`，拒绝本身留审计），来源检查
  先于配置、意图、进程或网络设置的任何写入；仅界面点击事件显式传递用户操作来源。
- `net_fix(action)` 工具名与参数不变：合法 action 返回 `manual-required` 与界面
  操作指引；未知 action 返回参数错误。依赖 v1「调用即修复」行为的集成需要适配。
- 移除启动、定时检查、快照刷新与通知点击中的自动清理/自动恢复；日志与通知
  状态仍正常保存。发现问题改为托盘提醒一次（15 分钟冷却），修复由用户点击。
- 通知修复：冷却时间戳持久化（重启后仍生效）；手动检查不再消耗自动提醒机会；
  通知标题使用显示名而非内部标识；兜底故障分类与解释表词汇对齐。
- 代理归属判断改为精确解析 (host, port)：不再使用子串/前缀匹配
  （`127.0.0.1:18080` 不再被误判为受控）；快照恢复新增可用性闸门
  （目标不可达即拒绝，防止恢复出死代理），回读必须包含 ProxyEnable。
- 界面：AppUserModelID 固定，任务栏/固定项正确关联图标；状态展示、明暗主题、
  托盘与既有交互保持不变。
- 隐私扫描 v2：通用规则（内置、可公开）+ 私有规则（仓库外，按需提供）两层；
  扫描器自身接受检查；覆盖跟踪/暂存/未跟踪候选集与根文件，二进制按字节扫描，
  无法读取的对象列入待处理；`--history` 支持历史提交补丁与身份元数据审计。

## 1.0.0（2026-09-19）

首个公开版本：四链路状态卡、静默定时检查、可审计的受限修复、MCP 接入。
